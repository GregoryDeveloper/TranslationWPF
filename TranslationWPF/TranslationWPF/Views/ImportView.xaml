<Window x:Class="TranslationWPF.Views.ImportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TranslationWPF.Views"
        mc:Ignorable="d"
        Title="ImportView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="4*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button x:Name="ImportBTN" Content="Import" Width="150" Height="50" VerticalAlignment="Top" Margin="10" Command="{Binding ImportCommand}" />
        </StackPanel>
        <DataGrid Grid.Row="1" Margin="10" ItemsSource="{Binding Translations}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Français" Binding="{Binding Language1}"/>
                <DataGridTextColumn Header="Commentaire" Binding="{Binding Language1Comment}"/>
                <DataGridTextColumn Header="Exemple" Binding="{Binding Language1Example}"/>
                <DataGridTextColumn Header="Synonsyms" Binding="{Binding Synonyms1String}"/>


                <DataGridTextColumn Header="English" Binding="{Binding Language2}"/>
                <DataGridTextColumn Header="Comentaire" Binding="{Binding Language2Comment}"/>
                <DataGridTextColumn Header="Exemple" Binding="{Binding Language2Example}"/>
                <DataGridTextColumn Header="Synonsyms" Binding="{Binding Synonyms2String}"/>


                <DataGridTextColumn Header="Ligne" Binding="{Binding Line}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
